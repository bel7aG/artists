import styled from 'styled-components'

export const SMask = styled.span`
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: 100%;
  z-index: 91100011;
  /*
    I use the red when i want to DEBUG IT (WHERE MAGICS HAPPEN)
  */
  background-color: red;
  cursor: default;
`
